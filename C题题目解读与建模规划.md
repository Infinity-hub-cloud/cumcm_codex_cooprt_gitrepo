# 2026 国赛 C 题：题目解读与建模规划

> 适用对象：建模、编程、论文三名队员及各自的 Codex。  
> 文档状态：题面与附件已完成只读核查，10 项关键口径已由人工确认。  
> 当前边界：本文仅统一题意、决策口径和后续规划；**尚未建立、求解或验证任何预测模型与优化模型，也没有生成比赛结果。**

## 1. 一页结论

本题研究一个“光伏 + 储能 + 外部电网”的微网购电调度问题。核心不是单纯求一个最低电价时段，而是在以下三条时间链上做一致决策：

1. **预测链**：只能使用决策时刻之前已经出现的数据，预测未来负载、光伏和波动电价；
2. **调度链**：根据当时可见的预测结果制定计划购电、储能充放电以及后续调整；
3. **结算链**：真实负载、真实光伏和真实电价只在实际发生后用于执行、紧急购电、费用结算和下一轮训练，不能倒灌到此前决策中。

四问是逐层递进的：

| 问题 | 主要新增难点 | 计划时刻 | 关键输出 |
|---|---|---|---|
| 问题 1 | 确定性单日经济调度 | 当日 0:00 | 计划购电、储能充放电、当日费用 |
| 问题 2 | 负载与光伏未知，需因果预测；预测误差触发紧急购电 | 每日 0:00 | 全年逐日计划、紧急购电、连续 SOC |
| 问题 3 | 0/6/12/18 时获得新版光伏预测，可滚动调整购电 | 每日 0/6/12/18 时 | 初始计划、最终调整量、调整惩罚、紧急购电 |
| 问题 4 | 电价也未知且实时波动，需预测未来价格 | 同问题 2/3 | 波动电价下的两套重算结果 |

整体建议采用“**因果时序预测 + 储能约束优化 + 逐时滚动仿真 + 事后真实值结算**”的统一框架。问题 1 可先作为物理约束和费用计算的确定性基线；问题 2--4 在此基础上增加预测、在线更新和结算机制。

## 2. 数据与提交结构

### 2.1 权威来源

原始题面和附件当前位于：

`D:\Users\LENOVO\Desktop\数学建模\正式比赛C题`

共享仓库不复制或修改原始附件；后续任何派生数据均应记录来源、处理规则和版本。

### 2.2 已核实的数据结构

| 文件 | 数据内容 | 时间范围与粒度 |
|---|---|---|
| `附件1.xlsx` | 电价、负载、光伏预测 | 单日，144 个 10 分钟区间 |
| `附件2.xlsx` | 真实小区负载、真实光伏功率 | 2025-01-01--2025-12-31，每日 144 段 |
| `附件3.xlsx` | 0/6/12/18 时发布的未来 24 小时光伏预测 | 365 天 × 4 次/天 × 24 个整点 |
| `附件4.xlsx` | 真实波动电价 | 2025 全年，每日 144 段 |
| `附件5/` | `result1`、`result2`、`result3`、`result4-2`、`result4-3` 模板 | 规定工作表、日期、时间段和汇总列 |

已核实：附件 1、2、4 的日内时间列从 `0:10` 到 `0:00+1`，对应以区间终点标记的 144 个 10 分钟段；附件 2、4 覆盖 365 天；附件 3 每天四行，只有每日第一行填写日期，读取时需在派生数据中向下补齐日期，但不得改动原文件。

### 2.3 时间和单位统一

设一天分为 $T=144$ 个区间，区间长度

$$
\Delta t=\frac{1}{6}\ \text{h}.
$$

负载和光伏输入单位为 kW，购电、充放电和储电量输出单位为 kWh。必须先做

$$
\text{区间能量}=\text{区间功率}\times\Delta t,
$$

严禁将 kW 数值直接填入 kWh 结果表。

时间索引统一采用“右端点”口径：数据列 `0:10` 表示区间 $[0{:}00,0{:}10]$，`0:00+1` 表示 $[23{:}50,24{:}00]$。

## 3. 人工已确认的十项决策

以下内容是后续建模、编程和论文共同遵守的固定口径；若需改变，必须重新经过人工确认并同步修改本文。

1. **问题 2 禁止未来信息泄漏。** 每日 0:00 只能使用此前历史数据，不能使用当日真实负载、真实光伏。应从 ARIMA、LSTM、移动平均等候选方法中构建并比较时序预测模型，不限于这些模型。
2. **问题 1 的 SOC 首尾固定。** $S_0=6000$ kWh，且 $S_{24:00}=6000$ kWh。
3. **问题 2--4 连续逐日滚动。** SOC 跨日连续传递，不强制每天 24:00 回到 6000 kWh。
4. **充放电变量采用电网侧口径。** 充电量是从微网母线进入电池前的能量，放电量是电池向微网母线输出的能量。
5. **整点光伏预测的基准映射为小时内恒定。** 预测的整点功率直接复用于对应小时内 6 个 10 分钟段；梯度或插值仅作为后续可比较的改进方案。
6. **`调整购电量`填写最终调整后购电量 $Q$**，不是增量 $Q-G$。
7. **计划量高于调整量的 50% 违约项是非负惩罚支出**，不是退款或收益。
8. **问题 4 禁止预知当日真实电价。** 每日 0:00 只能使用历史电价预测未来价格；实际价格发生后再用于真实结算和后续更新。
9. **基线模型禁止同一时段同时充电和放电。** 只有在能严格证明最优解天然互斥时，才可去掉整数互斥变量并采用连续松弛。
10. **精度规则固定。** 内部求解使用高精度浮点数；输入和原始派生变量保持 4 位小数；论文表格展示 2 位小数；Excel 结果写入 4 位小数；全天汇总必须先对未舍入时段值求和，再统一舍入。

术语说明：本文将问题 2 称为“**逐日滚动预测**”，即每天 0:00 重新利用截至前一日的历史数据预测当天；问题 3 是“**日内滚动调整**”，即在 6/12/18 时根据新发布的光伏预测再次修正尚未执行的计划。两者不能混淆。

## 4. 统一物理与费用框架

### 4.1 变量建议

对日期 $d$、10 分钟区间 $t$，建议统一使用：

- $l_{d,t}$、$p_{d,t}$：真实负载能量、真实光伏能量，kWh；
- $\hat l_{d,t|k}$、$\hat p_{d,t|k}$：决策时刻 $k$ 可见的预测值；
- $\pi_{d,t}$、$\hat\pi_{d,t|k}$：真实电价与预测电价，元/kWh；
- $G_{d,t}$：每日 0:00 制定的计划购电量，kWh；
- $Q_{d,t}$：问题 3/4-3 最终调整后购电量，kWh；
- $E_{d,t}$：紧急购电量，kWh；
- $C_{d,t}$、$D_{d,t}$：电网侧充电量、放电量，kWh；
- $S_{d,t}$：区间末储能电量，kWh；
- $W_{d,t}$：无法消纳的剩余能量或弃能量，kWh，仅在处理预测过量时用于保证物理可行，并必须单独统计。

### 4.2 储能约束

题面参数：储能额定容量 12000 kWh，允许运行区间 1200--10800 kWh，最大充放电功率 5000 kW，效率 90%。在电网侧变量口径下，基准递推为

$$
S_{d,t}=S_{d,t-1}+\eta_c C_{d,t}-\frac{D_{d,t}}{\eta_d},
$$

其中基线取 $\eta_c=\eta_d=0.9$。单个 10 分钟区间有

$$
0\le C_{d,t}\le 5000\Delta t=833.3333\ \text{kWh},
$$

$$
0\le D_{d,t}\le 833.3333\ \text{kWh},
$$

$$
1200\le S_{d,t}\le10800.
$$

基线用二进制状态约束充放电互斥，例如

$$
C_{d,t}\le833.3333z_{d,t},\qquad
D_{d,t}\le833.3333(1-z_{d,t}).
$$

问题 1 固定 $S_{1,0}=S_{1,144}=6000$；问题 2--4 则满足

$$
S_{d,0}=S_{d-1,144},
$$

形成全年连续状态链。

### 4.3 能量平衡

若以实际执行购电量 $X_{d,t}$ 表示进入微网的常规购电，则实现层应满足

$$
X_{d,t}+E_{d,t}+p_{d,t}+D_{d,t}
=l_{d,t}+C_{d,t}+W_{d,t}.
$$

问题 1/2 中通常 $X=G$，问题 3 中 $X=Q$。$W$ 只用于处理预测偏高造成的实际过剩；其含义、来源构成和总量必须报告，不能借弃能变量掩盖过度购电。题面没有明确允许向外网售电，因此基线不设置售电收益。

### 4.4 规划与真实执行必须分离

后续程序应显式分为两层：

1. **规划层**：只读取决策时刻可见的历史和预测值，输出 $G$ 或更新后的 $Q$；
2. **执行层**：按时间推进后才读取真实负载、真实光伏和真实价格，计算储能实际动作、紧急购电、弃能及真实费用。

如果同一函数在制定 0:00 计划时可访问当天完整真实数组，即构成未来信息泄漏，即便最终结果数值很好也不能接受。

## 5. 问题 1：确定性单日基准

### 5.1 题干实质

问题 1 已直接给出当日电价、负载和光伏预测，不要求额外训练预测模型。目标是在满足负载和储能约束的前提下利用峰谷价差移峰填谷，并确保日末 SOC 回到初始水平。

### 5.2 建模规划

基线目标函数为

$$
\min\sum_{t=1}^{144}\pi_tG_t.
$$

约束包括：能量平衡、购电非负、SOC 递推和边界、充放电功率上限、充放电互斥、$S_0=S_{144}=6000$。

问题 1 最适合作为全项目的“单元测试”：先验证时间索引、kW 到 kWh、效率方向、SOC 首尾和费用汇总，再复用到后三问。应设置至少两个对照：无储能购电成本、使用储能后的购电成本。

### 5.3 论文解释重点

- 低价时充电、高价时放电的经济直觉；
- 效率损耗意味着并非所有微小价差都值得套利；
- 光伏高发时段优先本地消纳，储能吸收剩余；
- 闭环 SOC 保证策略可以日复一日重复，而不透支下一天。

## 6. 问题 2：因果预测下的逐日计划与紧急购电

### 6.1 题干实质

问题 2 的困难不是把真实负载和真实光伏直接代入优化，而是每天 0:00 用历史数据预测当天 144 个区间，并在预测误差存在时平衡“计划购电成本”和“5 倍电价紧急购电风险”。

对 2025-02-01 的计划，只允许使用 2025-01-31 及此前信息；对任意日期 $d$，只允许使用日期严格早于 $d$ 的数据。当天真实值只能在对应时段发生后进入执行层。

### 6.2 推荐流程

1. 用 2025 年 1 月作为初始预热/训练期，同时从 2025-01-01 00:00 的 6000 kWh 开始推进 SOC；
2. 在 2025-02-01 00:00 以前的数据上生成当天负载与光伏预测；
3. 用预测值制定 144 段计划购电和参考储能轨迹；
4. 按真实 10 分钟数据逐段执行，储能在约束内吸收偏差，剩余短缺形成紧急购电，剩余过量形成可审计弃能；
5. 当天结束后将已发生真实数据加入历史窗口，更新次日预测，SOC 连续传到次日；
6. 重复到 2025-12-31。

### 6.3 预测模型候选与选择原则

候选至少包括：季节性朴素/同周同刻、移动平均或指数平滑、ARIMA/SARIMA、带日内周期特征的回归/树模型、LSTM 等。模型名称不是得分点，严格的滚动验证和下游成本才是重点。

建议先建立低复杂度基线，再决定是否引入 LSTM。评价指标包括 MAE、RMSE、归一化 MAE；光伏含大量夜间 0，不能把普通 MAPE 作为唯一指标。最终选择还应比较预测进入调度后产生的总费用、紧急购电量和约束可行率。

### 6.4 费用规划

问题 2 的真实结算可写为

$$
J_2=\sum_{d,t}\left(\pi_tG_{d,t}+5\pi_tE_{d,t}\right),
$$

其中日内基准价格 $\pi_t$ 来自附件 1。必须区分预测时优化所用的预期目标和使用真实数据回放后得到的实际结算结果。

## 7. 问题 3：日内滚动预测与购电调整

### 7.1 题干实质

问题 3 在问题 2 的基础上增加 6:00、12:00、18:00 的新版光伏预测。每次只能修改尚未执行的区间；已经发生的购电、SOC、紧急购电和费用必须冻结。

附件 3 的“预报 1 小时”按区间终点解释：例如 6:00 发布的 1 小时预测对应 7:00 的功率，并作为 $[6{:}00,7{:}00]$ 内 6 个 10 分钟段的基准恒定功率。其余预测依次类推。插值或梯度重建只作为后续消融试验，不改变基准规则。

### 7.2 滚动优化规划

- 0:00：用 0:00 光伏预测和因果负载预测生成初始计划 $G$；
- 6:00：固定 0:00--6:00 已执行量，利用 6:00 新预测重算剩余时段并更新 $Q$；
- 12:00、18:00：重复同一原则；
- 每次优化可使用未来 24 小时窗口以降低日末 SOC 的短视效应，但当日提交只写当天对应区间；跨日状态必须连续。

### 7.3 调整费用

定义

$$
U_{d,t}^{-}=\max(G_{d,t}-Q_{d,t},0),\qquad
U_{d,t}^{+}=\max(Q_{d,t}-G_{d,t},0).
$$

根据人工确认的口径，总费用采用

$$
J_3=\sum_{d,t}\left[
\pi_tG_{d,t}
+0.5\pi_tU_{d,t}^{-}
+1.5\pi_tU_{d,t}^{+}
+5\pi_tE_{d,t}
\right].
$$

这里 $0.5\pi U^-$ 是惩罚支出，不冲减计划购电费用；`调整购电量` 工作表写 $Q$。程序应另存 $Q-G$、$U^-$、$U^+$ 供费用审计，但不改变模板定义。

### 7.4 “是否需要更多预报时刻”的回答方式

不能只凭直觉回答。至少比较：

- 仅 0:00 计划；
- 使用题定 0/6/12/18 时预测；
- 在不伪造新预测数据的前提下，用插值/误差情景模拟更高更新频率的敏感性方案。

比较总费用、计划费用、调整惩罚、紧急购电量、弃能量、预测误差和计算耗时。若额外更新减少的紧急成本不足以覆盖调整惩罚，就不能声称更新越频繁越好。

## 8. 问题 4：波动电价预测下的重算

### 8.1 题干实质

附件 4 是真实历史/结算价格，不是每天 0:00 可提前获知的未来价格。问题 4 必须增加一个因果电价预测模块，然后分别重做问题 2 和问题 3。

### 8.2 问题 4-2

每日 0:00 用截至前一时刻的历史价格预测未来 144 段价格，再与负载、光伏预测共同制定计划。真实价格发生后用于结算：

$$
J_{4-2}=\sum_{d,t}\left(\pi_{d,t}G_{d,t}+5\pi_{d,t}E_{d,t}\right).
$$

优化时使用 $\hat\pi_{d,t|0}$，最终报告使用真实 $\pi_{d,t}$；两者不能混写。

### 8.3 问题 4-3

在 0/6/12/18 时滚动更新尚未执行区间。除新版光伏预测外，原则上可使用决策时刻之前已发生的真实价格更新剩余价格预测；不得读取未来真实价格。真实结算为

$$
J_{4-3}=\sum_{d,t}\left[
\pi_{d,t}G_{d,t}
+0.5\pi_{d,t}U_{d,t}^{-}
+1.5\pi_{d,t}U_{d,t}^{+}
+5\pi_{d,t}E_{d,t}
\right].
$$

电价预测同样需要朴素基线、传统时序模型和必要的复杂模型比较；最终看预测误差与调度经济性，而不是只看拟合曲线。

## 9. 严格的滚动验证协议

后续实验必须采用滚动起点或扩展窗口，禁止随机打乱全年时间点后切分训练/测试。建议协议如下：

1. 2025 年 1 月作为初始历史窗口；
2. 输出期为 2025-02-01--2025-12-31，共 334 天；
3. 日期 $d$ 的训练数据截止到 $d-1$；日内时刻 $k$ 的数据截止到 $k$；
4. 模型选择、超参数和安全裕度只能基于当时已发生的训练/验证数据；
5. 每天完成后才允许把当天真实数据加入下一日训练集；
6. 保留每个决策时刻的训练截止日期、预测版本、输入哈希、SOC 起点和输出版本；
7. 另算使用未来真实值的“Oracle 下界”时，必须显著标注为事后理想基准，不得冒充可执行策略。

## 10. 模型评价体系

### 10.1 预测层

- 负载/电价：MAE、RMSE、nMAE，必要时分时段和分季节统计；
- 光伏：全天 MAE、白昼 MAE、RMSE，避免夜间大量零值使指标失真；
- 稳定性：不同月份、极端峰值日和节假日代理时段的误差分布。

### 10.2 调度层

- 总购电费用及相对无储能基线的节省率；
- 计划购电费、调整惩罚费、紧急购电费的分解；
- 紧急购电总量、次数、连续时间段和最大单次量；
- 弃能/弃光量及光伏本地消纳率；
- SOC 最小值、最大值、边界触达次数和等效循环强度；
- 供需平衡残差、SOC 递推残差、充放电互斥违规数；
- 求解时间和不可行天数。

### 10.3 对照与消融

至少保留：无储能、朴素预测 + 优化、候选预测模型 + 优化、是否滚动调整、整点恒定与插值映射、是否使用安全裕度。任何不利结果也必须保留，不能只报告最优方案。

## 11. 关键建模注意事项

1. **最严重风险是数据泄漏。** 当天真实负载、光伏和价格不得进入当天 0:00 的预测或优化。
2. **预测精度不等于经济效果。** MAE 更低的模型未必带来更低的紧急购电费，应以完整闭环仿真判断。
3. **SOC 不能每日重置。** 问题 2--4 必须从前一天 24:00 状态继续。
4. **一月不是可丢弃数据。** 它既是预测预热期，也是从 6000 kWh 开始建立连续 SOC 的前置仿真期；正式模板从 2 月输出不等于状态从 2 月重新初始化。
5. **整点预测与 10 分钟索引容易错位。** 应对 0:00、6:00、跨日 24:00 做专门断言。
6. **费用必须分账。** 计划、向下调整惩罚、向上调整溢价、紧急购电分别计算后再汇总。
7. **不要先舍入再求和。** 全日、全年统计均从内部高精度值计算，最后按展示/文件规则舍入。
8. **禁止用负购电表示售电。** 题面未定义售电机制；如发生过量，只能按明确的储能或弃能规则处理。
9. **效率口径必须一致。** 电网侧 $C,D$、SOC 方程、结果表和论文文字不得混用电池侧定义。
10. **模板中的省略号不是数据行。** 输出时应根据官方模板允许的填法扩展或填写，不得把 `⁝` 当作日期或数值。
11. **不可行与异常必须显式记录。** 不得通过删天、截断负值或放宽 SOC 边界掩盖。
12. **LSTM 不是默认优选。** 数据只有一年，复杂模型必须通过严格滚动验证证明其增益，否则采用更稳健、可解释的模型。

## 12. 尚需在实现前显式落盘的次级假设

10 项核心问题已通过，但以下细节仍应在真正建模前写入数据字典或模型假设表：

- “充放电效率 90%”是否按充、放电各 90% 解释；当前基准如此处理；
- 预测过量时总剩余能量如何区分弃光和已购电弃置，及其是否增加次级惩罚；
- 问题 2 的实际执行阶段采用何种储能纠偏策略，或是否直接用两阶段/鲁棒优化表达；
- 问题 3 多次调整的惩罚是否始终相对 0:00 原计划结算；当前依据人工结论按 $G$ 与最终 $Q$ 结算；
- 问题 4 在 6/12/18 时是否允许用当天已经发生的真实价格更新剩余价格预测；当前按“允许使用已经发生信息、禁止未来信息”规划；
- 是否引入储能寿命/循环退化成本。题面未给参数，基线不强行加入；可在敏感性分析中讨论。

这些是“需在实现前声明的模型假设”，不影响本次题目解读通过，也不授权立即开始建模。

## 13. 团队分工接口

### 建模成员

负责锁定符号、目标函数、约束、预测与优化衔接、对照实验和评价指标。任何新假设先写入模型说明，再交给编程实现。

### 编程成员

负责只读解析附件、构建因果滚动数据集、实现断言、运行预测/优化和按模板导出。程序必须在接口上隔离“可见历史”和“未来真实值”。

### 论文成员

负责将问题重述、假设、模型、算法、结果和局限写成完整论证。不得把候选方案写成已采用方案，不得把 Oracle 基准写成实际策略，不得把预测告警或模型输出写成已验证事实。

建议三方通过简短交接文件同步：数据版本、模型版本、运行编号、指标表、异常清单和人工确认状态，不直接覆盖他人分区文件。

## 14. 当前验收状态与下一闸门

- **已确认事实**：题面与附件结构已只读核查；10 项关键口径已由人工明确；本文已形成统一题目解读和建模规划。
- **本轮未做事项**：未构建预测器，未编写优化模型，未进行数据处理、训练、调参、求解、绘图或结果填表。
- **当前阶段**：题目解读文档已生成，等待团队人工审阅。
- **下一步人工动作**：三名队员审阅本文，重点检查第 3、4、6、7、8、12 节是否与共同理解一致。
- **下一阶段通过条件**：人工明确指定下一阶段任务、允许修改的唯一分区和产物；在此之前，各 Codex 不得自行进入模型构建或实验运行。
