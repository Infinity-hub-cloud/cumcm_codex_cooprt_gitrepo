# 工作区整理核验报告（2026-09-12）

## 结论

共享区已建立“总导航 → 分区导航 → 证据区导航 → 全量文件清单”的四级索引。当前事实状态统一为：Q1 已通过；Q2 `weekday_buffer` 已人工接受并冻结，但论文表转录和比赛提交批准仍单独待确认；Q3 修订版代码已就绪、待人工全年运行；Q4 未开始。

## 实体整理

- 唯一移动：根目录 `编程.zip` 移至 `编程/归档/历史压缩快照/编程_历史快照_整理前_20260912.zip`。
- 移动前后 SHA-256 均为 `1CD36270FFF91BC12D4899410D151DF4E5DBC0AF2A0E53124DAFA3C4A6C37D2C`。
- 正式附件、源码、配置、测试、预处理数据、`runs/` 和人工日志均保持原路径。
- 没有删除历史失败包、旧语义 Q3 包或缓存；缓存只在资产清单中标记为非交付文件。
- 新增根目录 `.gitignore`，只忽略 Python 缓存、pytest 缓存和本地虚拟环境；未删除或重置既有已跟踪缓存。

## 导航更新

- 更新原有项目 README：共享区、建模、编程、论文、Q2 候选包、旧冒烟审计归档。
- 新增导航：C题、B题、项目管理、运行证据、人工日志、候选区、审计区、归档区、历史压缩快照及三个预处理目录。
- 第三方 `论文/cumcm-paper-agent-skills/` 的 README 按供应方内容保持不变，由论文分区 README 解释其边界。

## 自动核验

- 项目文件清单覆盖：585/585（最终核验时；排除 `.git/`，包含生成缓存）。
- 相对路径唯一：585/585。
- 空 SHA 字段：0；清单和摘要自身 2 行使用 `SELF_NOT_HASHED`。
- 项目自有 README：18 份。
- README 相对链接：检查 34 个，缺失 0。
- 历史压缩快照：目标存在、根目录旧路径不存在、哈希一致。
- 管理脚本：Python 语法检查通过。

文件数可能随本报告及之后的人工运行增加；最新准确计数以 `资产状态摘要_20260912.json` 为准。运行完成后应重新执行清单生成与链接检查，并在总导航补登记新的 `q3_revised_*` 状态。

## 未执行事项

本轮没有调用正式 Solver，没有运行 Q3 全年轨道，没有生成新的 `result3_candidate.xlsx`，没有更改官方 Excel，没有进入问题 4，也没有执行 Git commit/push/pull/reset/clean。

下一步：人工严格按 `编程/Q3_人工运行说明.md` 在全新 `runs/q3_revised_<timestamp>/` 下运行改进后的代码。
