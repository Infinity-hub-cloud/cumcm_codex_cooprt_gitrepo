# 建模分区规则

仅当 Codex 要做题意拆解、变量与假设、目标与约束、评价体系、模型比较或优化路线时读取本文件。

- 放入：确认后的题意解释、模型卡、指标定义、约束与假设、路线比较、给编程手的实现规格、给论文手的模型说明。
- 不放入：原始附件副本、可执行代码、运行日志、论文全文或排版文件。
- 每份结论写清题号/子问题、输入来源、版本、状态（待确认或已确认）和交接对象。
- 需要编程实现时，只交付输入输出、变量单位、目标、约束、参数、验证和预期结果；不修改 `编程/`。

## 阶段状态索引

- Phase 1：问题 1 物理 MILP 规格已完成，尚未求解；版本 `M1-Q1-MILP-v1.3-instant-left-hold`。
- Phase 1 总结：四问统一基线规格已确立，见根目录 `四问模型完整基线.md`；缺陷与改进登记见 `当前基线缺陷与待改进备选方案.md`。
- 已确认口径：参数化的 $T$ 个时间区间（当前快照为 144 个 10 分钟区间）；母线侧充放电；$\eta_c,\eta_d$ 当前均取 0.9；问题 1 的 $S^{\mathrm{ini}},S^{\mathrm{term}}$ 均取 6000 kWh；问题 2--4 跨日连续 SOC；MILP 二进制互斥；不售电；主目标和惩罚费用均通过符号化参数接口表达。题面常数只在参数快照中赋值，主公式不直接嵌入数值。
- 当前状态：`CONFIRMED-SPEC / NO-RUN-RESULTS`。尚未构建预测器、优化代码、训练、求解、回放、调参或填表。
- 已关闭：`BLOCKER-TIME-001`。附件 1/2/4 的标签是瞬时时刻，按左端点零阶保持映射到随后 10 分钟；模型和五个模板均严格从当日 `0:10-0:20` 到次日 `0:00-0:10`，不得平移或修改标签。
- 下一阶段闸门：修订版正式审计已在 `编程/预处理审计输出_时间口径修订版/` 完成并返回 `PASS`；当前等待人工核验报告与规范化数据，确认后才授权生成模型代码。
