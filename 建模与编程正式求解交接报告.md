# 2026 国赛 C 题：数据预处理完成后的建模与编程正式求解交接报告

> 交接日期：2026-09-11（Asia/Shanghai）  
> 交接阶段：阶段 4 数据预处理与审计结束，转入阶段 5 实现规格与逐问基线代码  
> 当前状态：可开始公共底座和问题 1；问题 2 在问题 1 人工通过后开始；问题 3 与问题 4-3 在首次实现前必须关闭本文的 `BLOCKER-Q3-TIME-001`  
> 执行边界：Codex 生成和审查代码；人工安装/确认环境、运行、调参、查看完整日志、导出结果并决定是否通过  
> 结果边界：截至本报告生成时，尚无预测、优化、求解、回放或正式填表结果

## 1. 本报告的用途和权威顺序

本报告面向下一位负责正式编程的队友 Codex。目标不是重新讨论题意，而是把当前已经形成的模型基线、修订后的数据规则、程序接口、时间语义、运行证据和人工回填要求固化为一个可直接执行的交接入口。

遇到冲突时按以下顺序处理：

1. 官方 `C题.pdf`、附件 1--5；
2. 人工在本项目中明确确认的规则；
3. 本报告；
4. `四问模型完整基线.md`；
5. `C题题目解读与建模规划.md`；
6. `建模/阶段1_问题1基线模型.md` 与 `建模/阶段1_交接.md`；
7. 其他历史理解、草稿或烟测产物。

若低优先级文件与高优先级文件冲突，不得静默择一。先停止受影响问题的实现，登记 `BLOCKER`，说明影响范围，再交人工决定。

## 2. 当前团队建模状态

### 2.1 已完成并可继承

- 官方题面、四个输入附件和五个结果模板已经盘点并建立 SHA-256 清单。
- 四问共用的储能物理层、信息可见性、费用结构、滚动机制、参数接口和评价方向已经形成基线规格。
- `BLOCKER-TIME-001` 已关闭：附件 1、2、4 的标签按瞬时时刻解释，并映射到随后 10 分钟；官方模板标签不改动。
- 修订版只读预处理已生成规范化 CSV、审计报告、机器可读摘要和输入清单。
- 修订版审计状态为 `PASS`：441/441 项检查通过，错误 0，告警 0。
- 本次交接复核重新运行了 12 项预处理单元测试，全部通过。
- 本次交接使用允许共享读取的方式重新计算当前源文件哈希，11/11 个文件与正式审计清单一致。
- 原始数值精度复核显示附件 1--4 的数值单元格均不超过 4 位小数，规范化 CSV 的 4 位写出没有损失当前输入精度。

### 2.2 尚未完成

- 尚未实现预测器、优化器、滚动回放、费用结算和正式导出器。
- 尚未选择负载、光伏、电价的最终预测模型及超参数。
- 尚未运行任何问题的正式基线或候选模型。
- 尚未产生可填入论文或 `result*.xlsx` 的数值结果。
- 尚未取得求解状态、MIP gap、运行时间、预测误差、鲁棒性或经济性指标。
- 附件 3 自然日时钟网格与模板窗口的连接规则仍有一个必须显式关闭的缺口，见第 8 节。

### 2.3 Git 与文件状态

- 共享仓库当前分支为 `main`，跟踪 `origin/main`，当前 `HEAD` 为 `e5225f9`。
- 工作树不是干净状态：存在团队成员的已修改、已删除和未跟踪文件，且当前预处理代码、审计输出和部分基线文档尚未全部进入该提交。
- 下一位 Codex 不得执行 `git reset --hard`、`git clean`、批量恢复或覆盖操作，也不得把 `HEAD` 当成当前完整状态。
- 每次运行应记录实际文件哈希；在团队明确要求前不要擅自提交、推送或清理其他成员的改动。

## 3. 开始编程前必须阅读的材料

按顺序、按需读取，禁止把旧训练题或过期产物混入本题：

1. `../workflow.md`：人机边界、阶段门禁和结果证据规则。
2. 本报告：当前状态、实现顺序、接口和回填文件。
3. `四问模型完整基线.md`：四问统一数学规格。
4. `C题题目解读与建模规划.md`：题意、已确认的十项规则、指标体系。
5. `编程/预处理审计输出_时间口径修订版/audit/audit_report.md`。
6. `编程/预处理审计输出_时间口径修订版/audit/audit_summary.json`。
7. `编程/预处理审计输出_时间口径修订版/audit/manifest.json`。
8. 当前问题所需的规范化 CSV；不要一次加载全部大表。
9. 做问题 1 时再读 `建模/阶段1_问题1基线模型.md` 与 `建模/阶段1_交接.md`。
10. 进入受控优化前再读 `当前基线缺陷与待改进备选方案.md`，不要在基线尚未运行时堆叠复杂模型。

明确禁止作为正式输入或现行规则使用：

- `模拟训练1/` 中的任何数据、代码、结果和结论；
- `编程/预处理审计输出/`：这是旧时间语义产物，已过期；
- `编程/_smoke_audit_time_revision_20260911_b/` 与 `_c/`：仅是技术烟测；
- `正式比赛C题/理解.md` 中把 `0:10` 当作前一段区间终点的旧解释；该解释已被后续人工确认和修订版文档取代；
- Excel 自动生成的 `~$*.xlsx` 锁文件；
- 模板中的 `⁝`：它是排版省略符，不是日期、数值或可保留的数据行。

## 4. 正式数据版本与预处理结果

### 4.1 唯一正式派生数据目录

后续模型默认只读取：

`编程/预处理审计输出_时间口径修订版/normalized/`

原始文件仍是最高权威来源，但不应在每个模型模块中重复解析。若规范化数据与原始附件发生冲突，停止运行并回到预处理审计，不得在模型代码中临时修补。

数据版本建议固定为：

`PREP-TIME-REV-20260911T170805+0800`

关键指纹：

| 对象 | SHA-256 |
|---|---|
| `audit/manifest.json` | `E489553660EB610271F2B192272BF1EB94BDE3D88CECE3D40048792349EE1B06` |
| `audit/audit_summary.json` | `95F8E17FC0428EFAA4B2BD256D2E6B51C7F5066B2835C9F6B17EB88D3A916AED` |
| `preprocess_audit.py` | `7A65118808D495393E0EA8EDFC3B9C5F750766F0DE428FB5FEC6D189AE6AE0DC` |
| `test_preprocess_audit.py` | `FD3AAA5542C6A2CD8AFEBC0F92F506FC94CC0C76AC1543E7C0E43173D10F5BEE` |

### 4.2 附件与规范化产物

| 来源 | 已核实结构与范围 | 正式规范化文件 | 数据行数 |
|---|---|---|---:|
| 附件 1 | `145×4`；1 个表头 + 144 个瞬时样本；价格 0.3713--1.3952 元/kWh；负载 3309.3934--5958.9696 kW；预测光伏 0--7612.316 kW | `attachment1_single_day.csv` | 144 |
| 附件 2 | 两张 `366×145` 工作表；2025-01-01--2025-12-31；负载和实际光伏各 365×144；负载 1995.7176--7978.8849 kW；光伏 0--10216.2 kW | `attachment2_actual_long.csv` | 52,560 |
| 附件 3 | `1461×26`；365 天×4 个发布时间；每次 24 个整点预测；日期只在每日首行出现，派生时向下补齐 | `attachment3_forecast_long.csv` | 35,040 |
| 附件 3 映射 | 每个整点预测展开为 6 个 10 分钟钟表段 | `attachment3_mapped_10min.csv` | 210,240 |
| 附件 4 | `366×145`；365×144 个实际电价；范围 0.0076--1.7936 元/kWh；负价格数量 0 | `attachment4_actual_price_long.csv` | 52,560 |

正式 CSV 指纹：

| 文件 | SHA-256 |
|---|---|
| `attachment1_single_day.csv` | `695E14DD02D011E24AE8CE1C5CD7BFF4CA963F5C92FA884BACC0386059AD07E3` |
| `attachment2_actual_long.csv` | `00DA155528AFC173776F59E620C8AEFCD661A5C5D8520472CE3AA9F0A91631ED` |
| `attachment3_forecast_long.csv` | `B9F67A02CC979DDE70E9045E7B18A32DC9B1FBCC002C23AEB04DAAA697452328` |
| `attachment3_mapped_10min.csv` | `5830C5F598039F765CDDA4C70E8B14FE152AC7C7D3BDDA031737A98CAB5D8DFD` |
| `attachment4_actual_price_long.csv` | `71EE47345986E960F41B8841C9C339D3931573A02CBD61C44C19848DFBB3DE1A` |

### 4.3 审计已经证明的内容

- 所有必需文件存在，工作表集合、维度和关键表头满足当前规则。
- 附件 1、2、4 的样本分钟严格为 10、20、…、1440。
- 附件 2 和附件 4 各覆盖 365 个无重复日期。
- 附件 3 覆盖 365 天，每天均有 0:00、6:00、12:00、18:00 四次发布记录。
- 五个结果模板名称、工作表集合、骨架维度、日期首尾和 144 个计划/调整购电标签均通过检查。
- 所有被审计的原始工作簿和模板公式单元格数量均为 0。
- 当前附件 4 没有负价格，因此 `PRICE-NEG-001` 在当前数据版本中不触发；代码仍必须保留负价格断言和停止机制。

注意：`audit_summary.json` 中每项检查的真实结果字段是 `passed`。例如 `attachment3_record_order` 的 `passed=true`，其 `detail` 文本使用的是失败情形说明，不应把该句误读为实际乱序。

## 5. 修订后的统一时间接口

### 5.1 必须区分三种时间索引

为避免附件 3 与模板发生静默错位，程序中禁止只保留一个含义不明的 `interval_index`。统一使用：

| 字段 | 定义 | 范围/例子 |
|---|---|---|
| `template_date` | 官方计划/调整购电表的一行日期 | 2025-02-01--2025-12-31 |
| `template_slot` | 模板行内位置，从当日 0:10 起算 | 1--144 |
| `clock_slot` | 自然日从 0:00 起算的钟表段位置，仅用于自然日网格或附件 3 | 1--144 |
| `interval_start` | 带日期的物理区间起点 | ISO 8601 datetime |
| `interval_end` | 带日期的物理区间终点 | ISO 8601 datetime |
| `decision_time` | 制定或更新计划的真实时刻 | ISO 8601 datetime |
| `information_cutoff` | 该次预测/决策允许读取的最新真实数据时刻 | ISO 8601 datetime |

跨来源合并的唯一安全主键是：

```text
(interval_start, interval_end)
```

`source_date + interval_index`、`target_date + interval_index` 或裸 `interval_index` 都不能作为附件 2/4 与附件 3 的直接连接键。

### 5.2 附件 1、2、4 和模板规则

- 原始 `0:10` 是瞬时时刻，左端点保持到 `[0:10,0:20]`。
- 原始 `0:00+1` 是次日 0:00，左端点保持到次日 `[0:00,0:10]`。
- `template_slot=1` 对应 `[template_date 00:10, 00:20]`。
- `template_slot=143` 对应 `[template_date 23:50, template_date+1 00:00]`。
- `template_slot=144` 对应 `[template_date+1 00:00, 00:10]`。
- 一个模板行是从当日 0:10 到次日 0:10 的 24 小时标签窗口，不是自然日 0:00--24:00。
- 功率必须乘 `delta_t=1/6 h` 后变成区间电量；不得把 kW 直接写入 kWh 输出。

### 5.3 自然日汇总与跨日动作

所有时序动作应先按物理 `interval_start` 保存，再生成模板视图。自然日 `d` 的 `0:00-4:00` 充放电汇总不能只从模板行 `d` 截取：

- `[d 00:00,d 00:10]` 来自模板行 `d-1` 的 `template_slot=144`；
- `[d 00:10,d 04:00]` 来自模板行 `d` 的 `template_slot=1..23`。

因此问题 2--4 的 1 月预热不仅用于预测训练和 SOC 推进，也为 2 月 1 日 0:00--0:10 的自然日汇总提供前序动作。正式输出期开始时不得重置 SOC 或丢弃该段。

### 5.4 SOC 时间点

- `S_before[d,t]`：区间开始时 SOC。
- `S_after[d,t]`：区间结束时 SOC。
- 数组形式可使用 `S[0..T]`，其中 `S[0]` 对应模板窗口起点当日 0:10，`S[T-1]` 对应次日 0:00，`S[T]` 对应次日 0:10。
- 问题 1：`S[T-1]=6000 kWh` 且 `S[T]=S[0]`，用于落实自然日 0:00/24:00 相同及周期闭合。
- 问题 2--4：`S[d,T]=S[d+1,0]`，全年连续，不得逐日重置为 6000 kWh。

## 6. 共同遵守的数据、变量与参数接口

### 6.1 数据层字段

模型层读取后应形成统一长表，至少包含：

```text
template_date
template_slot
clock_slot
interval_start
interval_end
sample_time_label
sample_minute
load_kw
pv_actual_kw
price_actual_yuan_per_kwh
load_kwh
pv_actual_kwh
forecast_issue_time
information_cutoff
forecast_source
forecast_version
load_pred_kw
pv_pred_kw
price_pred_yuan_per_kwh
```

并遵守：

- 真实量、确定输入和预测量不得共用同一列名。
- 预测记录必须携带 `forecast_issue_time`、`information_cutoff` 和版本，便于自动检查未来信息泄漏。
- 所有日期时间在内存中使用真正的 datetime，不用字符串排序决定先后。
- 导出 CSV 时使用 ISO 8601；时区固定为本地比赛时间语义，不在中途做 UTC 偏移。

### 6.2 决策与状态变量

| 程序字段 | 数学符号 | 单位 | 含义 |
|---|---|---:|---|
| `grid_plan_kwh` | $G_{d,t}$ | kWh | 0:00 制定的初始计划购电量 |
| `grid_final_kwh` | $Q_{d,t}$ | kWh | 问题 3/4-3 最终调整后的购电量 |
| `grid_committed_kwh` | $X_{d,t}$ | kWh | 统一平衡式中实际承诺量，按问题取 $G$ 或 $Q$ |
| `grid_emergency_kwh` | $E_{d,t}$ | kWh | 真实回放中的紧急购电量 |
| `charge_bus_kwh` | $C_{d,t}$ | kWh | 微网母线侧进入电池前的充电量 |
| `discharge_bus_kwh` | $D_{d,t}$ | kWh | 电池向微网母线侧输出的放电量 |
| `soc_before_kwh` | $S_{d,t-1}$ | kWh | 区间前 SOC |
| `soc_after_kwh` | $S_{d,t}$ | kWh | 区间后 SOC |
| `spill_kwh` | $W_{d,t}$ | kWh | 未被负载或储能吸收的剩余能量 |
| `charge_mode` | $z_{d,t}$ | 0/1 | 充放电互斥变量 |
| `adjust_down_kwh` | $U^-_{d,t}$ | kWh | `max(G-Q,0)` |
| `adjust_up_kwh` | $U^+_{d,t}$ | kWh | `max(Q-G,0)` |

统一物理式：

$$
X_{d,t}+E_{d,t}+p_{d,t}+D_{d,t}
=l_{d,t}+C_{d,t}+W_{d,t},
$$

$$
S_{d,t}=S_{d,t-1}+\eta_cC_{d,t}-\frac{D_{d,t}}{\eta_d}.
$$

### 6.3 题面参数配置

配置文件中至少包含：

```text
interval_count = 144
delta_t = 1/6
day_horizon = 24
hourly_forecast_step = 1
update_times = [00:00, 06:00, 12:00, 18:00]
charge_efficiency = 0.9
discharge_efficiency = 0.9
soc_min = 1200
soc_max = 10800
charge_power_max = 5000
discharge_power_max = 5000
soc_initial = 6000
soc_terminal = 6000
emergency_multiplier = 5
downward_adjustment_multiplier = 0.5
upward_adjustment_multiplier = 1.5
warmup_start_date = 2025-01-01
output_start_date = 2025-02-01
output_end_date = 2025-12-31
```

题面参数、预测器参数、求解器参数、随机种子和数值容差必须分层保存，不能混在一个无来源字典中。代码不得散落硬编码 0.9、5000、5、0.5、1.5 等数值。

### 6.4 费用字段

每个时段和每天都单独保存：

```text
planned_purchase_cost
downward_adjustment_penalty
upward_adjustment_cost
emergency_purchase_cost
total_cost
```

向下调整的 50% 是非负惩罚支出，不是退款。所有费用用未舍入的时段变量计算，最后才做展示舍入。

## 7. 共同代码架构与不可越过的边界

建议公共代码分为：

```text
raw_reader
data_contract
time_index
forecast
optimize
replay
settle
metrics
export_templates
run_manifest
tests
```

硬边界：

- `forecast` 只能接收 `information_cutoff` 以前的数据。
- `optimize` 只能接收预测量、当前 SOC、可用电价预测和参数，不能访问未来真实数组。
- `replay` 才能按时间顺序读取已经发生的真实值。
- `export_templates` 只能读取通过断言的内部结果；不能重新计算模型或修正异常。
- Oracle 必须使用独立入口和独立结果目录，文件名含 `oracle`，不得与可执行策略混合。
- 原始附件只读；任何输出写入新目录，正式模板也只能复制后填充。
- 任何运行失败、不可行、非最优终止、告警或负面比较都必须保留。

## 8. 编码前必须关闭或显式登记的事项

### 8.1 `BLOCKER-Q3-TIME-001`：附件 3 与模板窗口的 10 分钟错位

当前正式派生文件中：

- 附件 2/4 的 `interval_index=1` 对应 `[00:10,00:20]`；
- 附件 3 映射表的 `interval_index=1` 对应自然日 `[00:00,00:10]`；
- 0:00 发布的 24 小时预测覆盖 `[d 00:00,d+1 00:00]`；
- 模板行 `d` 覆盖 `[d 00:10,d+1 00:10]`。

因此二者只有 143 个 10 分钟区间按物理时间直接重合：附件 3 多出开头 `[00:00,00:10]`，模板多出末尾次日 `[00:00,00:10]`。现有 `attachment3_mapped_10min.csv` 适合描述自然日钟表区间，但其 `interval_index` 不能直接当 `template_slot`。

推荐的最低风险处理是：

1. 所有匹配按 `interval_start/interval_end` 做，不按裸索引做；
2. 对 0:00 初始计划中缺少的末尾 10 分钟，使用问题 2 已建立的因果光伏预测器作为单段回退，并把 `forecast_source=fallback_q2_pv` 写入结果；
3. 问题 3 的最终 $Q$ 在次日 0:00 可利用届时刚发布且因果可见的新预测更新该尚未执行区间，但不得反向修改已经执行的区间；
4. 将这一规则写入配置、单元测试和运行清单。

以上是本报告的推荐方案，不是已有人工确认事实。开始问题 3/4-3 正式代码前，人工必须明确回复“采用推荐方案”或给出替代规则。问题 1、问题 2 和问题 4-2 不受此门禁阻塞。

### 8.2 模板骨架扩展

审计通过的是官方空白模板骨架，不是最终结果文件尺寸。`计划购电量` 和 `调整购电量` 已有 334 个日期行；`充放电量`、`紧急购电量` 中的 `⁝` 需要在导出副本中替换为完整结果：

- `充放电量`：按 334 天逐日生成 6 个四小时汇总及 0:00/24:00 SOC；
- `紧急购电量`：把连续的正紧急购电段合并成区间，逐日输出 0 个或多个事件；
- 不得把 `⁝` 原样留在正式结果中，也不得把它计入日期或行数断言；
- 导出器必须回读最终文件，按内容规则验收，而不能仍用空模板的固定骨架行数验收。

### 8.3 基线中必须显式保留的假设

- 充、放电效率分别取 0.9，往返效率为 0.81；“总往返效率 0.9”只做敏感性方案。
- 不允许售电，购电量非负。
- 当前不设置外部电网购电容量上限。
- 基线不计储能退化成本，但必须统计总吞吐量和等效循环代理。
- 基线将剩余能量统一记为 `spill_kwh`；后续可拆分弃光和已购电弃置。
- 问题 2--4 的主回放先采用 R0：固定计划储能动作，短缺由紧急购电补足，过剩记为弃能。更强 recourse 必须作为后续单因素候选。
- 问题 3/4-3 的费用按最终 $Q$ 相对 0:00 初始 $G$ 结算；逐次调整路径只做内部审计。

## 9. 逐问代码生成、人工运行和通过门禁

总体顺序：公共底座 → 问题 1 → 问题 2 → 问题 3 → 问题 4-2 → 问题 4-3。问题 4 虽是一个题号，但实现和验收应拆为两个独立运行轨道。

### 9.0 公共底座

Codex 先生成：

- 参数配置和数据版本读取；
- 物理时间轴与模板视图转换；
- 未来信息泄漏断言；
- 通用 MILP 构造、回放、结算、指标、运行清单和导出验证模块；
- 最小单元测试，不启动正式求解。

人工返回：

```text
00_common/test_report.txt
00_common/interface_snapshot.json
00_common/environment.json
00_common/human_feedback.md
```

通过条件：时间轴、单位、配置、源数据哈希和关键测试均通过；无模型数值结果要求。

### 9.1 问题 1：确定性单日基线

必须先运行：

- B0 无储能基线；
- B1 带二进制充放电互斥的 MILP；
- 若出现多重最优，再由人工决定是否增加“最小总吞吐量”词典序次目标。

人工返回：

```text
01_q1/run_manifest.json
01_q1/config_snapshot.json
01_q1/dispatch_timeseries.csv
01_q1/metrics_summary.csv
01_q1/assertions.json
01_q1/solver.log
01_q1/warnings_and_failures.log
01_q1/result1_candidate.xlsx
01_q1/export_validation.json
01_q1/human_feedback.md
```

通过条件：求解状态和 gap 合格；能量平衡、SOC、边界、互斥、目标重算、全天守恒和 Excel 回读全部通过；`J_B1 <= J_B0 + tolerance`。不能要求储能方案总购电量一定更低。

### 9.2 问题 2：因果预测、逐日计划和真实回放

先实现完全因果、低复杂度预测基线，再实现候选模型接口。模型胜负必须由滚动验证和下游费用共同决定，不按名称或单一 MAE 决定。

最低要求：

- 从 2025-01-01 6000 kWh 开始预热和推进 SOC；
- 2025-02-01--2025-12-31 为正式 334 天输出期；
- 日期 $d$ 的 0:00 只使用严格早于 $d$ 的真实数据；
- 同时保留无储能、朴素预测+优化和当前候选+优化；
- 每天结束后才把当天真实数据加入下一日历史。

人工返回：

```text
02_q2/run_manifest.json
02_q2/config_snapshot.json
02_q2/model_selection.csv
02_q2/predictions_load.csv
02_q2/predictions_pv.csv
02_q2/dispatch_timeseries.csv
02_q2/daily_metrics.csv
02_q2/monthly_metrics.csv
02_q2/metrics_summary.csv
02_q2/assertions.json
02_q2/solver_days.csv
02_q2/warnings_and_failures.log
02_q2/result2_candidate.xlsx
02_q2/export_validation.json
02_q2/human_feedback.md
```

通过条件：无未来泄漏；334 天状态连续；物理断言全部通过；预测与调度指标齐全；异常天、不可行天和失败模型未被删除；正式候选优于或至少不劣于预注册基线，若不改善则保留基线。

### 9.3 问题 3：日内滚动光伏更新与购电调整

开始前先关闭 `BLOCKER-Q3-TIME-001`。随后复用问题 2 的负载预测和全年状态链，实现 0:00 初始 $G$、6/12/18 时对未执行区间的更新及最终 $Q$。

人工返回：

```text
03_q3/run_manifest.json
03_q3/config_snapshot.json
03_q3/forecast_alignment_audit.csv
03_q3/plan_versions.csv
03_q3/predictions_pv_by_issue.csv
03_q3/dispatch_timeseries.csv
03_q3/daily_metrics.csv
03_q3/monthly_metrics.csv
03_q3/metrics_summary.csv
03_q3/assertions.json
03_q3/solver_updates.csv
03_q3/warnings_and_failures.log
03_q3/result3_candidate.xlsx
03_q3/export_validation.json
03_q3/human_feedback.md
```

通过条件：每次更新只改尚未执行区间；已执行动作、SOC 和费用被冻结；`调整购电量` 写最终 $Q$；内部保留 $Q-G,U^-,U^+$；0:00-only 与四时点滚动在同一规则下比较；调整收益必须同时考虑调整惩罚、紧急购电和计算成本。

### 9.4 问题 4-2：波动电价下重做问题 2

复用问题 2 的数据、预测、回放和状态链，增加因果价格预测。优化使用预测价格，真实结算使用已发生的附件 4 价格。

人工返回：

```text
04_q4_2/run_manifest.json
04_q4_2/config_snapshot.json
04_q4_2/model_selection_price.csv
04_q4_2/predictions_price.csv
04_q4_2/dispatch_timeseries.csv
04_q4_2/daily_metrics.csv
04_q4_2/monthly_metrics.csv
04_q4_2/metrics_summary.csv
04_q4_2/assertions.json
04_q4_2/solver_days.csv
04_q4_2/warnings_and_failures.log
04_q4_2/result4-2_candidate.xlsx
04_q4_2/export_validation.json
04_q4_2/human_feedback.md
```

通过条件：价格预测无未来泄漏；当前数据无负价且有界性断言通过；价格误差与下游真实结算费用同时报告；与问题 2 的差异只归因于已登记的价格模块和波动价格结算。

### 9.5 问题 4-3：波动电价下重做问题 3

在问题 3 已通过和 `BLOCKER-Q3-TIME-001` 已关闭后，叠加因果滚动价格预测。每个更新时刻只用此前已经发生的价格更新剩余时域。

人工返回：

```text
05_q4_3/run_manifest.json
05_q4_3/config_snapshot.json
05_q4_3/model_selection_price.csv
05_q4_3/predictions_price_by_issue.csv
05_q4_3/forecast_alignment_audit.csv
05_q4_3/plan_versions.csv
05_q4_3/dispatch_timeseries.csv
05_q4_3/daily_metrics.csv
05_q4_3/monthly_metrics.csv
05_q4_3/metrics_summary.csv
05_q4_3/assertions.json
05_q4_3/solver_updates.csv
05_q4_3/warnings_and_failures.log
05_q4_3/result4-3_candidate.xlsx
05_q4_3/export_validation.json
05_q4_3/human_feedback.md
```

通过条件：同时满足问题 3 的非预见性和问题 4 的价格因果性；所有费用分账可重算；与问题 3、问题 4-2 的比较使用相同日期、初始状态、物理参数和指标口径。

## 10. 人工必须回填的性能指标

### 10.1 所有问题共同指标

| 类别 | 指标 |
|---|---|
| 经济性 | 计划购电费、调整惩罚/溢价费、紧急购电费、总费用、相对无储能节省率、相对基线变化 |
| 能量 | 计划/最终/紧急购电量、充电量、放电量、总吞吐量、弃能量、光伏本地消纳率 |
| 状态 | SOC 最小/最大、边界触达次数、首末/跨日连续残差、等效循环代理 |
| 物理正确性 | 最大能量平衡残差、最大 SOC 递推残差、累计残差、互斥违规数、负值违规数 |
| 求解 | 状态、终止原因、MIP gap、运行时间、不可行日期数、超时日期数 |
| 可追溯 | 数据版本、代码版本/哈希、配置哈希、环境、随机种子、运行编号 |

### 10.2 预测准确性

- 负载：MAE、RMSE、nMAE、峰值时段 MAE/最大误差，按月和季节分组。
- 光伏：全天 MAE、白昼 MAE、RMSE、爬坡时段误差；普通 MAPE 不得作为唯一指标。
- 电价：MAE、RMSE、峰谷排序一致率、高价时段召回率、经济加权绝对误差。
- 所有预测：样本数、有效覆盖率、偏差、误差分位数、极端日表现。

### 10.3 鲁棒性与稳定性

至少回填：

- 月度/季节分层指标，而不只给全年平均；
- 极端负载日、低光伏日、高价日的单独结果；
- 不同随机种子的均值、标准差和最差值（仅对含随机性的模型）；
- 扩展窗口与固定窗口的敏感性；
- 效率、初始 SOC、预测安全裕度等参数的小范围单因素敏感性；
- 失败率、不可行率、超时率和指标缺失率；
- 候选相对基线的逐日费用差分分布，而不是只报告总和。

每轮优化只改变一个主要因素。默认最多三轮；若主要经济指标没有稳定改善或物理/计算代价明显变差，应停止并保留基线。

## 11. 标准结果文件字段

### 11.1 `run_manifest.json`

至少包含：

```text
run_id
question_id
run_status
data_version
input_manifest_sha256
code_version_or_hashes
config_sha256
started_at
finished_at
environment
solver_name
solver_version
predictor_versions
random_seeds
information_cutoff_policy
output_period
git_head
git_dirty
warnings_count
failures_count
```

### 11.2 `dispatch_timeseries.csv`

至少包含：

```text
template_date,template_slot,clock_slot,interval_start,interval_end,
decision_time,information_cutoff,
load_actual_kwh,load_pred_kwh,pv_actual_kwh,pv_pred_kwh,
price_actual,price_pred,
grid_plan_kwh,grid_final_kwh,grid_emergency_kwh,
charge_bus_kwh,discharge_bus_kwh,spill_kwh,
soc_before_kwh,soc_after_kwh,charge_mode,
adjust_down_kwh,adjust_up_kwh,
planned_purchase_cost,downward_adjustment_penalty,
upward_adjustment_cost,emergency_purchase_cost,total_cost,
balance_residual,soc_residual,solver_status
```

不适用字段留空，不得用 0 冒充“未计算”。

### 11.3 `metrics_summary.csv`

统一长表格式：

```text
run_id,question_id,model_id,baseline_id,split,period,
metric,value,unit,direction,sample_count,status,notes
```

### 11.4 `human_feedback.md`

人工每阶段至少填写：

```text
阶段/问题：
运行编号：
实际运行命令：
是否完整运行：
关键文件是否可打开：
断言是否全通过：
异常、警告和失败：
主要指标：
与基线比较：
人工结论：通过 / 退回修复 / 保留基线 / 进入单因素优化
允许的下一步：
确认人和确认时间：
```

## 12. 导出正式模板前的统一规则

- 原始模板永不覆盖；只生成 `*_candidate.xlsx`，人工验收后再复制为最终提交名。
- `计划购电量`、`调整购电量` 填 4 位小数；全天量和费用先用未舍入值累计再写入。
- 论文表格可展示 2 位小数，但不能反向覆盖程序结果。
- 问题 3/4-3 的“调整购电量”填写最终量 $Q$，不是 $Q-G$。
- 紧急购电事件应由连续正值区间合并得到，合并前后总量必须一致。
- 充放电四小时汇总按自然日时钟聚合，不按模板行简单切片。
- 最终回读核对：工作表名、日期、144 个标签、行列结构、单位、空值、公式、数值精度、全天汇总、事件总量和原模板未被修改。

## 13. 下一位 Codex 的首个任务

不要一次生成四问全部代码。首个任务只做公共底座和问题 1：

```text
当前为阶段 5，数据预处理已完成。请先阅读 ../workflow.md、
建模与编程正式求解交接报告.md、四问模型完整基线.md、
编程/预处理审计输出_时间口径修订版/audit/audit_summary.json，
再按需读取 attachment1_single_day.csv 和建模/阶段1_问题1基线模型.md。

本轮只生成公共时间/参数/断言底座和问题 1 的 B0+B1 可运行代码、测试、
运行说明与结果文件模板；不要运行正式模型，不要填写原始 result1.xlsx，
不要开始问题 2。输出必须符合本交接报告第 9.0、9.1、11、12 节。
若接口或时间语义冲突，停止并登记 BLOCKER，不得静默选择。
```

人工运行问题 1 并回传完整结果包后，队友 Codex 先诊断和关闭当前问题的门禁，再由人工决定是否进入问题 2。

## 14. 当前交接结论

```text
当前阶段：阶段 5 - 实现规格与逐问基线代码
当前状态：公共底座和问题 1 可开始；其后逐问推进；问题 3/4-3 有一个时间对齐门禁
已确认事实：修订版预处理 PASS；441/441 检查通过；12/12 单元测试通过；11/11 当前源文件哈希一致；尚无模型结果
待人工动作：将本报告交给队友 Codex；问题 1 代码生成后由人工运行并回传第 9.1 节结果包；问题 3 前确认 BLOCKER-Q3-TIME-001 的处理规则
进入下一阶段的条件：当前问题的代码、测试、运行清单、断言、性能指标、异常记录和候选模板均经人工确认
```
