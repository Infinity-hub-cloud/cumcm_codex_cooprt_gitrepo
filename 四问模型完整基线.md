# 2026 国赛 C 题：四问模型完整基线（最终确立版）

> 文档版本：`M-ALL-BASELINE-v1.0-symbolic-spec`  
> 文档状态：四问基线规格已确立，尚未实施、训练、求解或填表  
> 适用对象：建模、编程、论文成员及人工复核者  
> 下一闸门：数据预处理与人工复核、审计  
> 禁止事项：在数据审计通过前生成预测/优化模型代码、运行求解器或写入正式结果模板

## 1. 文档定位与证据边界

本文在题面、附件结构、人工确认的十项口径及问题 1 符号化 MILP 基线上，统一锁定问题 1、问题 2、问题 3、问题 4-2 和问题 4-3 的基线框架。本文的“最终确立”仅表示**模型规格与接口已形成当前团队主线**，不表示任何数值结果已经产生或通过验证。

权威顺序为：

1. 官方题面与附件；
2. 人工明确确认的十项口径；
3. 本文；
4. 后续经人工验收的数据审计报告、代码实现和运行记录。

若后续数据审计发现题面、附件、模板与本文存在冲突，应停止实现，将冲突标记为 `BLOCKER` 并交人工决定，不得静默修改数据、时间索引或模型口径。

本文严格区分四类状态：

- `CONFIRMED`：题面或人工复核已经明确；
- `BASELINE`：本阶段采用的可复现主线，后续可通过对照试验改进；
- `ASSUMPTION`：题面未完全说明，但实现前必须显式采用的假设；
- `BLOCKER/TODO`：尚未关闭的问题或后续工作，不得伪装为完成。

## 2. 四问本质与信息可用性

### 2.1 附件角色

| 附件 | 核心内容 | 基线用途 | 是否可被当日 0:00 直接完整使用 |
|---|---|---|---|
| `附件1.xlsx` | 单日分时电价、负载、光伏预测 | 问题 1 的确定性输入；问题 2、3 的固定日内电价曲线 | 问题 1 可；问题 2、3 的固定价格曲线可 |
| `附件2.xlsx` | 2025 年真实负载、真实光伏 | 问题 2--4 的历史训练数据与逐时真实回放/结算数据 | 只能使用决策时刻之前已经发生的部分；当日未来真实值不可用 |
| `附件3.xlsx` | 每日 0:00、6:00、12:00、18:00 发布的未来光伏预测 | 问题 3、4-3 的滚动光伏预测输入 | 只能使用当前及此前已经发布的版本；未来发布时间的版本不可用 |
| `附件4.xlsx` | 2025 年真实波动电价 | 问题 4 的历史价格训练数据与逐时真实结算价格 | 只能使用决策时刻之前已经发生的价格；当日未来真实价格不可用 |
| `附件5/` | 五个结果模板 | 人工确认后按模板导出 | 仅作为输出结构，不是模型输入 |

### 2.2 各问的电价、光伏和负载口径

| 问题 | 电价 | 光伏 | 负载 | 决策与状态 |
|---|---|---|---|---|
| 问题 1 | `附件1` 当日曲线，确定且可直接使用 | `附件1` 当日预测，确定输入 | `附件1` 当日负载，确定输入 | 单日确定性优化；日初、日末 SOC 均受约束 |
| 问题 2 | `附件1` 固定日内曲线，每天相同 | 用 `附件2` 已发生历史因果预测；当天真实值仅回放使用 | 用 `附件2` 已发生历史因果预测；当天真实值仅回放使用 | 每日 0:00 计划，按真实值逐段回放；SOC 跨日连续 |
| 问题 3 | `附件1` 固定日内曲线，每天相同 | 0:00/6:00/12:00/18:00 依次使用 `附件3` 当时已发布版本；真实值来自 `附件2` 回放 | 仍由 `附件2` 已发生历史因果预测 | 日内滚动调整未执行区间；SOC 跨日连续 |
| 问题 4-2 | 用 `附件4` 已发生历史因果预测；未来真实价格仅结算 | 与问题 2 相同 | 与问题 2 相同 | 问题 2 框架叠加价格预测 |
| 问题 4-3 | 在各决策时刻仅用已发生价格更新剩余价格预测；未来真实价格仅结算 | 与问题 3 相同 | 与问题 3 相同 | 问题 3 框架叠加价格滚动预测 |

`CONFIRMED`：问题 2--4 的实际负载、实际光伏和问题 4 的实际价格均不得作为同一决策时刻的未来已知量。完美预知只允许作为事后理论参考，不是可执行策略。

## 3. 统一符号与参数接口

### 3.1 索引与集合

| 符号 | 含义 | 单位 |
|---|---|---:|
| $\mathcal D$ | 仿真日期集合 | 日 |
| $d$ | 日期索引 | 日 |
| $\mathcal D^{\mathrm{out}}$ | 正式统计与填表日期集合 | 日 |
| $\mathcal T=\{1,\ldots,T\}$ | 单日离散时段集合 | 段 |
| $t$ | 单日时段索引 | 段 |
| $\Delta t$ | 单个时段长度 | h |
| $H$ | 单日调度长度，满足 $T\Delta t=H$ | h |
| $\sigma_t=t\Delta t$ | 附件 1/2/4 第 $t$ 个瞬时样本时刻，$t=1,\ldots,T$ | h |
| $I_{d,t}=[d+\sigma_t,d+\sigma_t+\Delta t]$ | 样本时刻映射到的随后区间 | h |
| $\mathcal U$ | 日内允许更新计划的时刻集合 | h |
| $u$ | 当前日内决策时刻 | h |
| $\Delta h$ | 附件 3 整点预测的时间粒度 | h |
| $\kappa=\Delta h/\Delta t$ | 一个整点预测对应的模型时段数 | 段 |

日期与时段必须组成唯一键 $(d,t)$。对每个模板日期 $d$，$t=1$ 对应 $[d+0{:}10,d+0{:}20]$，$t=T$ 对应 $[d+1\text{ 日 }0{:}00,d+1\text{ 日 }0{:}10]$，故整个标签窗口为 $[d+0{:}10,d+1\text{ 日 }0{:}10]$。所有程序数组下标、原始单元格、工作表标签和物理区间均须在数据审计报告中给出双向映射。

### 3.2 原始功率、区间能量与价格

| 符号 | 含义 | 单位 |
|---|---|---:|
| $L_{d,t}$、$P_{d,t}$ | 附件 1/2/4 在时刻 $\sigma_t$ 的负载、光伏瞬时功率 | kW |
| $l_{d,t}=L_{d,t}\Delta t$ | 零阶保持后，区间 $I_{d,t}$ 的负载电量 | kWh |
| $p_{d,t}=P_{d,t}\Delta t$ | 零阶保持后，区间 $I_{d,t}$ 的光伏电量 | kWh |
| $\pi_{d,t}$ | 时刻 $\sigma_t$ 的电价，基线用于随后区间 $I_{d,t}$ | 元/kWh |
| $\widehat L_{d,t\mid u}$、$\widehat P_{d,t\mid u}$ | 决策时刻 $u$ 可获得的负载、光伏功率预测 | kW |
| $\widehat l_{d,t\mid u}$、$\widehat p_{d,t\mid u}$ | 对应区间预测电量 | kWh |
| $\widehat\pi_{d,t\mid u}$ | 决策时刻 $u$ 对未来时段的价格预测 | 元/kWh |

上标“真实/确定”和帽号“预测”不可混写。附件 1/2/4 的时间标签是瞬时时刻；基线以左端点零阶保持构造随后 10 分钟的区间量。实际值只能在对应时刻发生后进入回放、结算与下一次预测更新。

### 3.3 储能与费用参数

| 符号 | 含义 | 单位 |
|---|---|---:|
| $\eta_c$、$\eta_d$ | 充电效率、放电效率 | 1 |
| $S^{\min}$、$S^{\max}$ | 储能电量下限、上限 | kWh |
| $P_c^{\max}$、$P_d^{\max}$ | 最大充电、放电功率 | kW |
| $Q_c^{\max}=P_c^{\max}\Delta t$ | 单段最大母线侧充电量 | kWh |
| $Q_d^{\max}=P_d^{\max}\Delta t$ | 单段最大母线侧放电量 | kWh |
| $S^{\mathrm{ini}}$ | 全仿真起始储能电量 | kWh |
| $S^{\mathrm{term}}$ | 问题 1 要求的单日终端储能电量 | kWh |
| $\alpha_{\mathrm{emg}}$ | 紧急购电价格乘数 | 1 |
| $\alpha_{\mathrm{under}}$ | 向下调整惩罚价格乘数 | 1 |
| $\alpha_{\mathrm{over}}$ | 向上调整溢价价格乘数 | 1 |

所有题面常数均先注册为符号，再由独立参数快照赋值。主公式、程序模型约束、断言和费用函数不得直接散落硬编码数值。

### 3.4 统一决策变量

| 符号 | 类型 | 含义 | 单位 |
|---|---|---|---:|
| $G_{d,t}$ | 连续、非负 | 0:00 制定的初始计划购电量 | kWh |
| $Q_{d,t}$ | 连续、非负 | 问题 3/4-3 最终调整后的计划购电量 | kWh |
| $X_{d,t}$ | 连续、非负 | 统一平衡式中的已承诺计划购电量；按问题取 $G$ 或 $Q$ | kWh |
| $E_{d,t}$ | 连续、非负 | 真实回放中因短缺产生的紧急购电量 | kWh |
| $C_{d,t}$ | 连续、非负 | 微网母线侧进入储能前的充电量 | kWh |
| $D_{d,t}$ | 连续、非负 | 储能向微网母线输出的放电量 | kWh |
| $S_{d,t}$ | 连续 | 时段结束后的储能电量 | kWh |
| $W_{d,t}$ | 连续、非负 | 未被负载或储能吸收的剩余能量 | kWh |
| $z_{d,t}$ | 二进制 | 充放电互斥变量 | 1 |
| $U^-_{d,t}$ | 连续、非负 | 相对初始计划的向下调整量 | kWh |
| $U^+_{d,t}$ | 连续、非负 | 相对初始计划的向上调整量 | kWh |

$C$、$D$ 均为母线侧能量口径。充电后仅有 $\eta_cC$ 进入储能；母线得到 $D$ 时，储能必须减少 $D/\eta_d$。

## 4. 统一物理模型

### 4.1 区间能量平衡

真实执行层采用：

$$
X_{d,t}+E_{d,t}+p_{d,t}+D_{d,t}
=l_{d,t}+C_{d,t}+W_{d,t}.
$$

其中：

- 问题 1 取 $X_t=G_t$ 且 $E_t=0$；
- 问题 2、4-2 取 $X_{d,t}=G_{d,t}$；
- 问题 3、4-3 取 $X_{d,t}=Q_{d,t}$。

规划层使用决策时刻可见的预测量，并令紧急购电为零：

$$
X_{d,t}+\widehat p_{d,t\mid u}+D_{d,t}
=\widehat l_{d,t\mid u}+C_{d,t}+W_{d,t}.
$$

问题 1 的已知输入可视为预测误差为零的特殊情形。

### 4.2 SOC 递推

$$
S_{d,t}=S_{d,t-1}+\eta_cC_{d,t}-\frac{D_{d,t}}{\eta_d}.
$$

$$
S^{\min}\le S_{d,t}\le S^{\max}.
$$

### 4.3 充放电功率上限与互斥

$$
0\le C_{d,t}\le Q_c^{\max}z_{d,t},
$$

$$
0\le D_{d,t}\le Q_d^{\max}(1-z_{d,t}),
$$

$$
z_{d,t}\in\{0,1\}.
$$

`BASELINE`：同一时段禁止同时充电和放电。连续松弛只能作为后续对照；即使能够证明存在互斥的等价最优解，也不能默认求解器返回的每个松弛最优解都自然互斥。

### 4.4 首末状态与跨日连续性

本模型令 $S_{d,0}$ 表示标签窗口起点 $d$ 日 0:10 的 SOC，$S_{d,t}$ 表示区间 $I_{d,t}$ 结束后的 SOC。于是问题 2--4 的相邻标签窗口满足：

$$
S_{d,T}=S_{d+1,0},
$$

二者都对应 $d+1$ 日 0:10。正式输出期开始时不得重新初始化 SOC。

全仿真已知初值位于 2025-01-01 0:00。基线初始化桥接规则为：缺失的 $[0{:}00,0{:}10]$ 内令储能不充不放，从而

$$
S_{d_0,0}=S^{\mathrm{ini}}.
$$

该桥接段不伪造负载、光伏或价格，也不进入正式模板；通过时序插值补齐它只能作为单独的敏感性方案。

问题 1 的输入每天完全重复。为同时严格对应模板窗口并落实 0:00/24:00 SOC 相等，将 $t=T$ 的区间 $[24{:}00,24{:}10]$ 视为下一周期的 $[0{:}00,0{:}10]$，并施加

$$
S_{T-1}=S^{\mathrm{ini}}=S^{\mathrm{term}},\qquad S_T=S_0.
$$

其中 $S_{T-1}$ 是 24:00 的 SOC，$S_T=S_0$ 保证 24 小时周期闭合。问题 2--4 不设置逐日回到固定 SOC 的约束。

### 4.5 不售电与剩余能量

$$
G_{d,t}\ge0,\qquad Q_{d,t}\ge0,\qquad E_{d,t}\ge0,\qquad W_{d,t}\ge0.
$$

题面未定义上网售电机制，因此不允许用负购电表示售电，也不计售电收入。$W$ 仅保证供给过剩时物理可行，必须单独审计；它不能被用于掩盖不合理的额外购电或不受约束的负价格套利。

## 5. 问题 1：确定性单日 MILP 基线

### 5.1 输入与决策

- 电价、负载、光伏预测均直接读取 `附件1.xlsx`，其时间标签按瞬时时刻解释；
- 每个瞬时样本以零阶保持映射到随后长度为 $\Delta t$ 的区间，144 个决策严格对应模板从 `0:10-0:20` 到次日 `0:00-0:10` 的 144 个标签；
- 决策变量为 $G_t,C_t,D_t,S_t,W_t,z_t$；
- 不构建负载、光伏或电价预测模型。

### 5.2 目标函数

$$
\min J_1=\sum_{t\in\mathcal T}\pi_tG_t.
$$

约束为第 4 节全部物理约束，并额外满足 $S_{T-1}=S^{\mathrm{ini}}=S^{\mathrm{term}}$ 与周期闭合 $S_T=S_0$。该写法在不篡改模板标签的前提下，保证每个自然日 0:00 与 24:00 的 SOC 都等于题定值。

### 5.3 求解与审计要求

该模型是 MILP。二进制变量仅用于充放电互斥；Big-M 必须取物理上限 $Q_c^{\max}$、$Q_d^{\max}$，不得使用任意巨大常数。

问题 1 是后续四问共同物理层的单元测试，至少验证：

- 时间索引和 kW 到 kWh 转换；
- 能量平衡和 SOC 递推残差；
- 首末 SOC、容量和功率边界；
- 充放电互斥；
- 未舍入向量重算的目标值；
- 与无储能基线的费用比较。

## 6. 问题 2：因果预测、日计划与紧急购电基线

### 6.1 因果预测接口

定义在日期 $d$ 的 0:00 可见信息集为 $\mathcal I_{d,0}$。负载和光伏预测满足：

$$
\widehat l_{d,t\mid0}=\mathcal F_L(\mathcal I_{d,0};\theta_L),
$$

$$
\widehat p_{d,t\mid0}=\mathcal F_P(\mathcal I_{d,0};\theta_P).
$$

$\mathcal I_{d,0}$ 只包含日期严格早于 $d$ 的真实负载、真实光伏及相应日历特征。预测器类型和超参数 $\theta_L,\theta_P$ 必须通过滚动验证选择，不得利用输出期未来数据调参。

`BASELINE`：先保留一个低复杂度、完全因果、可复现的季节性朴素/历史同刻预测器作为预测基准；ARIMA/SARIMA、指数平滑、回归/树模型、LSTM 等只能在同一滚动协议下作为候选。具体窗口和超参数须在数据审计通过后单独登记。

### 6.2 每日计划优化

问题 2 的电价采用 `附件1` 的固定日内价格曲线。每天 0:00 以当前跨日 SOC 为初值，用预测负载和预测光伏求解：

$$
\min J^{\mathrm{plan}}_{2,d}
=\sum_{t\in\mathcal T}\pi_tG_{d,t},
$$

并在规划层满足第 4 节物理约束。该日计划不强制日末 SOC 回到 $S^{\mathrm{ini}}$，而是与次日连续。

### 6.3 真实回放与结算

基线回放按时间顺序执行已经制定的计划购电与储能动作。每个真实时段发生后，才读取 $l_{d,t},p_{d,t}$；计划不能覆盖未来。供给不足由 $E_{d,t}$ 补足，供给过剩由 $W_{d,t}$ 记录。

在固定该时段计划动作 $X_{d,t},C_{d,t},D_{d,t}$ 且不允许同时出现短缺和过剩的基线回放中，二者按正部计算：

$$
E_{d,t}=\max\left(l_{d,t}+C_{d,t}-X_{d,t}-p_{d,t}-D_{d,t},0\right),
$$

$$
W_{d,t}=\max\left(X_{d,t}+p_{d,t}+D_{d,t}-l_{d,t}-C_{d,t},0\right).
$$

这两个量代回第 4.1 节能量平衡恒等式。若后续采用储能实时纠偏或鲁棒 recourse，必须另建版本并与本 R0 基线对照，不能在回放代码中隐式改变计划动作。

真实结算为：

$$
J_2=\sum_{d\in\mathcal D^{\mathrm{out}}}
\sum_{t\in\mathcal T}
\left(\pi_tG_{d,t}+\alpha_{\mathrm{emg}}\pi_tE_{d,t}\right).
$$

正式输出期以前的预热期仍须按相同物理规则推进 SOC，但其费用是否进入正式统计必须服从模板和题面口径并在审计报告中单列。

## 7. 问题 3：日内滚动光伏预测与购电调整基线

### 7.1 附件 3 的因果使用与粒度映射

在更新时刻 $u\in\mathcal U$，只允许读取附件 3 中截至 $u$ 已发布的预测。设发布时刻 $u$ 的第 $r$ 个整点预测为 $\widehat P^{(3)}_{d,u,r}$，其覆盖区间为：

$$
[u+(r-1)\Delta h,\ u+r\Delta h].
$$

基线将该整点功率在对应的 $\kappa=\Delta h/\Delta t$ 个模型时段内保持恒定：

$$
\widehat P_{d,t\mid u}=\widehat P^{(3)}_{d,u,r}
$$

其中 $I_t$ 位于上述整点区间内。由此得到：

$$
\widehat p_{d,t\mid u}=\widehat P_{d,t\mid u}\Delta t.
$$

插值、梯度重建或形状约束曲线不是基线，只能作为后续敏感性方案。

### 7.2 滚动优化机制

在每日首个决策时刻 $u_0$，用当时可见的光伏预测和因果负载预测产生初始计划 $G_{d,t}$。在后续每个 $u\in\mathcal U$：

1. 冻结所有已执行时段的 $G,Q,C,D,S,E,W$ 和费用；
2. 以当前真实 SOC 作为剩余优化的初始状态；
3. 用当时已发布的新版光伏预测更新剩余未执行区间；
4. 负载仍使用决策时刻可得的因果预测；
5. 仅重算未执行区间，得到最终调整计划 $Q_{d,t}$；
6. 不允许任何更新追溯修改过去时段。

滚动子问题可用跨日预测窗口降低日末短视，但提交到当天模板的仍是当天时段，且跨日 SOC 连续。

### 7.3 调整量和费用

定义最终计划相对 0:00 初始计划的调整量：

$$
U^-_{d,t}=\max(G_{d,t}-Q_{d,t},0),
$$

$$
U^+_{d,t}=\max(Q_{d,t}-G_{d,t},0).
$$

等价线性化约束为：

$$
U^-_{d,t}\ge G_{d,t}-Q_{d,t},\qquad U^-_{d,t}\ge0,
$$

$$
U^+_{d,t}\ge Q_{d,t}-G_{d,t},\qquad U^+_{d,t}\ge0.
$$

在费用系数均非负时，最小化目标会使上述变量取对应正部。问题 3 的真实结算为：

$$
J_3=\sum_{d\in\mathcal D^{\mathrm{out}}}
\sum_{t\in\mathcal T}
\left[
\pi_tG_{d,t}
+\alpha_{\mathrm{under}}\pi_tU^-_{d,t}
+\alpha_{\mathrm{over}}\pi_tU^+_{d,t}
+\alpha_{\mathrm{emg}}\pi_tE_{d,t}
\right].
$$

$\alpha_{\mathrm{under}}\pi_tU^-_{d,t}$ 是非负惩罚支出，不是退款或收入。官方“调整购电量”工作表填写最终量 $Q$；内部另存 $Q-G$、$U^-$、$U^+$ 供审计。

## 8. 问题 4：波动电价下的两套基线

### 8.1 统一价格预测接口

对任一决策时刻 $u$，价格预测为：

$$
\widehat\pi_{d,t\mid u}=\mathcal F_\pi(\mathcal I_{d,u};\theta_\pi).
$$

$\mathcal I_{d,u}$ 只包含 $u$ 以前已经发生的真实价格及可用历史特征。未来真实价格 $\pi_{d,t}$ 不得进入当时的优化、模型选择或超参数调整。

`BASELINE`：先设置低复杂度因果价格基准，例如历史同刻/季节性朴素预测；ARIMA/SARIMA、指数平滑、回归/树模型、LSTM 等为候选。所有模型必须采用同一滚动验证协议，并同时比较价格误差和下游调度费用。

### 8.2 问题 4-2

问题 4-2 复用问题 2 的负载、光伏预测、每日计划和真实回放，只将规划电价替换为 0:00 可得的价格预测：

$$
\min J^{\mathrm{plan}}_{4-2,d}
=\sum_{t\in\mathcal T}\widehat\pi_{d,t\mid0}G_{d,t}.
$$

真实结算使用附件 4 中逐时发生的真实价格：

$$
J_{4-2}=\sum_{d\in\mathcal D^{\mathrm{out}}}
\sum_{t\in\mathcal T}
\left(
\pi_{d,t}G_{d,t}
+\alpha_{\mathrm{emg}}\pi_{d,t}E_{d,t}
\right).
$$

### 8.3 问题 4-3

问题 4-3 复用问题 3 的滚动光伏更新和购电调整。每个更新时刻 $u$ 可使用此前已经发生的真实价格更新剩余价格预测，但仍禁止读取未来真实价格。

真实结算为：

$$
J_{4-3}=\sum_{d\in\mathcal D^{\mathrm{out}}}
\sum_{t\in\mathcal T}
\left[
\pi_{d,t}G_{d,t}
+\alpha_{\mathrm{under}}\pi_{d,t}U^-_{d,t}
+\alpha_{\mathrm{over}}\pi_{d,t}U^+_{d,t}
+\alpha_{\mathrm{emg}}\pi_{d,t}E_{d,t}
\right].
$$

## 9. 滚动仿真与预测验证协议

### 9.1 时间顺序

1. 从全仿真起始日的 $S^{\mathrm{ini}}$ 开始；
2. 预热期按真实时间顺序推进，不丢弃其 SOC 影响；
3. 日期 $d$ 的 0:00 只读取严格早于 $d$ 的真实历史；
4. 生成问题 2/4-2 的日计划，或问题 3/4-3 的初始计划；
5. 每个时段发生后才读取该时段真实负载、光伏和真实价格；
6. 问题 3/4-3 只在 $u\in\mathcal U$ 更新尚未执行的区间；
7. 日末把 $S_{d,T}$ 原样传入次日；
8. 当日全部发生后，真实数据才能进入下一日训练窗口。

### 9.2 切分原则

- 禁止随机打乱全年时序后切分训练集和测试集；
- 采用扩展窗口或固定长度滚动窗口；
- 每次训练记录可见历史截止时刻、预测器版本、参数、输入哈希和随机种子；
- 模型选择与调参只能使用当时可见的训练/验证数据；
- 预测评估和调度评估均按月、季节、极值时段与全年汇总。

### 9.3 基线预测选择原则

预测器不能只按 MAE 排名。至少同时比较：

- 负载：MAE、RMSE、nMAE、峰值时段误差；
- 光伏：全天 MAE、白昼 MAE、RMSE、爬坡时段误差；
- 电价：MAE、RMSE、峰谷排序和高价时段识别能力；
- 下游：总费用、紧急购电量、弃能量、约束可行率与求解时间。

## 10. 对照组与理论参考

### 10.1 无储能对照

对每个适用问题保留同样信息约束、但令储能不参与的对照：

$$
C_{d,t}=D_{d,t}=0.
$$

无储能对照必须沿用对应问题的预测、滚动、调整和结算规则，不能把真实未来值偷偷引入其中。它用于衡量储能带来的边际价值。

### 10.2 完美预知 Oracle

可额外运行以下事后理论参考：

- 问题 2 Oracle：在 0:00 视同知道当天真实负载和真实光伏；
- 问题 3 Oracle：在相同物理约束下视同知道当天真实负载和真实光伏；
- 问题 4-2 Oracle：在 0:00 视同知道当天真实负载、真实光伏和真实波动电价；
- 问题 4-3 Oracle：在相同物理与滚动框架下视同知道未来真实负载、真实光伏和真实波动电价。

所有 Oracle 结果必须显著标注：**完美预知理论下界参考，不是可执行策略**。若费用结构、跨日终端处理或调整规则导致其不构成严格数学下界，只能称为“完美预知参考值”，不得强行宣称为下界。

## 11. 评价指标与必须保留的审计量

### 11.1 经济指标

- 计划购电费；
- 向下调整惩罚费；
- 向上调整溢价费；
- 紧急购电费；
- 总费用；
- 相对无储能和相对预测基准的费用变化；
- 与 Oracle 参考的可实现性差距。

### 11.2 能量与运行指标

- 计划购电量、最终调整购电量、紧急购电量；
- 总充电量、总放电量、弃能量和光伏本地消纳率；
- SOC 最小值、最大值、边界触达次数与跨日轨迹；
- 充放电互斥违规数；
- 能量平衡最大残差、SOC 递推最大残差和全期累计残差；
- 不可行日期、求解状态、MIP gap、运行时间和终止原因。

### 11.3 数值精度

- 内部求解与汇总使用浮点高精度、未舍入数据；
- 原始派生变量与读取的附件输入保留 4 位小数精度；
- Excel 结果文件保存 4 位小数；
- 论文表格可展示 2 位小数；
- 全天或全年总量必须先用未舍入时段数据求和，再进行最终舍入；
- 禁止先舍入每个时段再累计。

## 12. 当前参数快照

本节是唯一允许集中赋题面数值的地方。公式、模型接口和断言应引用符号或配置字段。

| 符号 | 当前快照 | 来源/状态 |
|---|---:|---|
| $H$ | 24 h | 题面 |
| $T$ | 144 | 题面及附件 |
| $\Delta t$ | $1/6$ h | 由 $T\Delta t=H$ |
| $\Delta h$ | 1 h | 附件 3 |
| $\kappa$ | 6 | 由 $\Delta h/\Delta t$ |
| $\mathcal U$ | 0:00、6:00、12:00、18:00 | 附件 3 |
| $\eta_c$、$\eta_d$ | 0.9、0.9 | 人工确认的基线解释 |
| $S^{\min}$、$S^{\max}$ | 1200、10800 kWh | 题面 |
| $P_c^{\max}$、$P_d^{\max}$ | 5000、5000 kW | 题面 |
| $Q_c^{\max}$、$Q_d^{\max}$ | $P_c^{\max}\Delta t$、$P_d^{\max}\Delta t$ | 派生参数 |
| $S^{\mathrm{ini}}$、$S^{\mathrm{term}}$ | 6000、6000 kWh | 问题 1 人工确认；后三问仅用全局初值 |
| $\alpha_{\mathrm{emg}}$ | 5 | 题面/人工确认 |
| $\alpha_{\mathrm{under}}$ | 0.5 | 题面/人工确认，非负惩罚支出 |
| $\alpha_{\mathrm{over}}$ | 1.5 | 题面/人工确认 |
| 预热期 | 2025-01-01 至 2025-01-31 | 当前基线规划 |
| $\mathcal D^{\mathrm{out}}$ | 2025-02-01 至 2025-12-31 | 官方输出期 |

## 13. 程序实现接口，但不授权本轮实现

后续实现必须分成相互隔离的五层：

1. `raw_reader`：只读原始附件，不覆盖源文件；
2. `preprocess_audit`：规范化日期、时间、单位与字段，并输出审计报告；
3. `forecast`：只接收当前决策时刻可见历史，输出带截止时刻的预测版本；
4. `optimize`：只接收预测量、当前 SOC 和符号化参数，不读取未来真实数组；
5. `replay_settle_export`：按时间顺序读取真实值、回放、结算，最终在人工确认后导出模板。

最小参数接口应包含：

```text
interval_count
delta_t
day_horizon
hourly_forecast_step
update_times
charge_efficiency
discharge_efficiency
soc_min
soc_max
charge_power_max
discharge_power_max
soc_initial
soc_terminal
emergency_multiplier
downward_adjustment_multiplier
upward_adjustment_multiplier
```

题面数值只允许在参数快照/配置层赋值。预测器参数、求解器参数、随机种子和数值容差必须另行登记，不得与题面参数混在同一层。

## 14. 输出模板与阻塞项

### 14.1 已知输出语义

- 问题 3 和问题 4-3 的“调整购电量”填写最终量 $Q$；
- 费用与汇总使用未舍入内部时段数据计算；
- 正式 Excel 写入保留 4 位小数；
- 程序必须回读导出文件，并核对工作表、日期、时段、行数、列数、单位和汇总值。

### 14.2 `BLOCKER-TIME-001` 已关闭

人工已明确确认：模板标签印刷无误，附件 1、2、4 的时间标签是瞬时时刻，必须映射到随后 10 分钟。故全部计划/调整购电表均严格按现有 144 个标签构造，跨度为当日 0:10 至次日 0:10；第一个数据格对应 $[0{:}10,0{:}20]$，最后一个数据格对应次日 $[0{:}00,0{:}10]$。

禁止采用旧的“模型 $[0{:}00,0{:}10]$ 顺序写入模板第一格”位置映射，禁止修改标签或默认官方印刷错误。`BLOCKER-TIME-001` 因权威时间口径明确而关闭。

## 15. 当前阶段与下一闸门

### 已完成

- 四问统一物理层、信息边界、费用结构和滚动机制已形成当前基线；
- 全部题面常数已转为符号化参数接口，并在独立快照集中赋值；
- 无储能和完美预知参考的角色已锁定；
- 当前缺陷与候选改进另见 `当前基线缺陷与待改进备选方案.md`。

### 尚未完成

- 原数据内容审计的旧派生输出使用“右端点”时间语义，已标记为过期；修订版正式输出已生成，当前待人工复核；
- 修订脚本已完成一次全新临时目录技术烟测（`PASS`、0 错误、0 警告）；该烟测不替代人工复核，临时输出不作为正式建模输入；
- 未确定最终预测器及其超参数；
- 未编写预测或优化模型代码；
- 未运行训练、求解、回放、调参、绘图或填表；
- 未产生任何可写入论文的实验结果。

### 下一阶段唯一任务

由人工核验已完成的修订版**只读数据预处理与审计输出**。重点确认附件 1/2/4 的瞬时时刻到随后 10 分钟映射、`0:00+1` 到次日首个 10 分钟的跨日日期、附件 3 映射及五个模板的完整标签序列。

只有人工明确确认审计通过后，才能进入模型代码生成阶段。
