# 旧冒烟审计归档

归档日期：2026-09-12；仅目录移动，没有永久删除，不改变正式输入和runs路径。

原路径均相对编程根目录：

| 原目录 | 归档位置 | 文件数 |
|---|---|---:|
| _smoke_audit_time_revision_20260911_a | 本目录内同名子目录 | 0 |
| _smoke_audit_time_revision_20260911_b | 本目录内同名子目录 | 8 |
| _smoke_audit_time_revision_20260911_c | 本目录内同名子目录 | 8 |

共保留16个文件、41761543字节；a为空的历史临时目录。逐文件指纹见archive_manifest.json。这些都是临时审计产物，不是当前正式输入。旧文件内部可能保留原绝对路径，该路径仅作历史记录，不回写历史manifest。

恢复：先确认编程根目录没有同名目录，再将所需子目录原名移回编程根目录。不要覆盖新生成目录。正式输入仍是`../../预处理审计输出_时间口径修订版/`。

旧`预处理审计输出/`及所有成功/失败runs均保留原位。历史`编程.zip`已按原 SHA-256 移入相邻的`../历史压缩快照/`，通过共享区 README 区分用途。
